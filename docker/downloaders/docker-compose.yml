---
# yaml-language-server: $schema=https://raw.githubusercontent.com/compose-spec/compose-spec/master/schema/compose-spec.json
services:
  sabnzbd:
    image: ghcr.io/linuxserver/sabnzbd:5.0.3
    container_name: sabnzbd
    restart: unless-stopped
    environment:
      - PUID=1000
      - PGID=1000
      - UMASK=002
      - TZ=America/New_York
    volumes:
      - /docker/appdata/sabnzbd/config:/config:rw
      ## Fix failed 7za return code 2 errors when unpacking certain nzbs
      - ./custom-init.sh:/custom-cont-init.d/99-fix-7z.sh
      - ./7zz-wrapper:/custom-cont-init.d/7zz-wrapper
      - /home/edumudu/storage/hd1/fake-nas/sabnzbd/data:/data:rw
    ports:
      - 8282:8080
 
  qbitty:
    image: linuxserver/qbittorrent:latest
    restart: unless-stopped
    container_name: qbitty
    # cap_add:
    #   - NET_ADMIN
    # sysctls:
    #
    #   - net.ipv4.conf.all.src_valid_mark=1
    environment:
      - PUID=1000
      - PGID=1000
      - WEBUI_PORT=8080
      # - VPN_ENABLED=true
      # - VPN_CONF=wg0
      # - VPN_PROVIDER=proton
      # - VPN_KEEP_LOCAL_DNS=false
      # - VPN_AUTO_PORT_FORWARD=true
      # - VPN_LAN_NETWORK=10.0.0.0/24,10.69.69.0/24

      # - VPN_LAN_LEAK_ENABLED=false
      # - VPN_FIREWALL_TYPE=auto
      # - PRIVOXY_ENABLED=false
      # - VPN_HEALTHCHECK_ENABLED=false
      # - UNBOUND_ENABLED=false
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /docker/appdata/qbitty:/config:rw
      - /home/edumudu/storage/hd1/fake-nas/qbitty/data:/data:rw
    ports:
      - 8080:8080
      - '62609:62609'
